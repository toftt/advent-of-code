import {part1, part2} from "./solution";

describe("{{year}} - day {{day}}", () => {
	it("solves part 1", () => {
		expect(part1()).toEqual(0);
	});

	it("solves part 2", () => {
		expect(part2()).toEqual(0);
	});
});